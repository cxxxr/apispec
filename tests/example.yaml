openapi: '3.0.1'
info:
  title: Simple API overview
  version: 2.0.0
paths:
  /foo:
    post:
      requestBody:
        content:
          application/json:
            schema:
              properties:
                foo:
                  type: string
      responses:
        '204':
          description: Successfully created a new user.
